import type { Component, MaybeReadable, WibbleSlots } from "@wibble/core";

export interface WeatherPageProps {
  slots?: WibbleSlots;
}

export declare const WeatherPage: Component<WeatherPageProps>;
export default WeatherPage;
