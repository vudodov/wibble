import type { Component, MaybeReadable, WibbleSlots } from "@wibble/core";

export interface CityControlsProps {
  slots?: WibbleSlots;
}

export declare const CityControls: Component<CityControlsProps>;
export default CityControls;
